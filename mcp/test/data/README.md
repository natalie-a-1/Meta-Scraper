Synthetic HEIF fixture: `synthetic.heic` is a 32 × 24 solid-color image created locally with Sharp and converted using macOS sips. Its GPS, author, title, and description are synthetic test values. No personal photo data is included. Tests also exercise the generic `mif1` HEIF major brand with the same encoded HEVC image. ExifTool image-data hashes verify that selective and full cleaning preserve the compressed image payload without requiring a platform HEVC decoder.
